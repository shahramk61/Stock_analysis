# Core data & numerics
yfinance>=0.2.40
pandas>=2.2
numpy>=1.26
statsmodels>=0.14
tabulate>=0.9

# Risk / regime
arch>=7.0.0
hmmlearn>=0.3.0

# Dashboard
streamlit>=1.28.0
plotly>=5.18.0

# ML / NLP (optional but used by default GPU signal path)
# Install torch with the CUDA wheel for your GPU if available, e.g.:
#   pip install torch --index-url https://download.pytorch.org/whl/cu128
torch>=2.4
transformers>=4.30.0          # FinBERT
chronos-forecasting>=1.0      # Zero-shot time series (Amazon Chronos)
neuralforecast>=1.7.0         # NHITS / TFT / PatchTST ensembles (opt-in via --forecasts)

# Dev (lightweight)
# pytest>=8.0   # optional; most tests are runnable as scripts
