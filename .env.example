# No secrets required for the primary path (local Python + Grok Build subscription).
#
# Optional: only if you deliberately use the external xAI HTTP client
# (scripts/agents/llm/grok_client.py) — NOT needed for /decide-stock.
# XAI_API_KEY=

# Optional: X/Twitter API for social sentiment helpers
# TWITTER_BEARER_TOKEN=
